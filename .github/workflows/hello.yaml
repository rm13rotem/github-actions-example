name: hello-actions

on:
  push:

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "Hello GitHub Actions"
        
  build:
    runs-on: ubuntu-latest
    needs: [ hello ]
    steps:
      - name: Print message
        run: echo "build image"
